# Simple Interest Calculator

This project contains a Bash script that calculates simple interest.

Input values:
- Principal
- Rate of interest
- Time period

Formula used:

SI = (Principal × Rate × Time) / 100wq
